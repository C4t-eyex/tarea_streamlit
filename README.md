# Instalación

Crear un entorno virtual: [Entorno virtual en VSC](https://code.visualstudio.com/docs/python/environments "Entorno virtual en VSC")

Ejecutar el comando ```pip install -r requirements.txt``` para instalar las librerias

# Levantar el proyecto

Para levantar el proyecto se debe de ejecutar el comando ```streamlit run src/__init__.py```